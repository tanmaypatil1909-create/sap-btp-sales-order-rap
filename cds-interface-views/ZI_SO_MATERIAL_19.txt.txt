@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Material Master Interface View'
@Metadata.ignorePropagatedAnnotations: true
@ObjectModel.usageType:{
  serviceQuality: #X,
  sizeCategory: #S,
  dataClass: #MIXED
}

define root view entity ZI_SO_MATERIAL_19
  as select from zso_material_19
{
      @ObjectModel.text.element: ['MaterialName']
  key material_id,

      material_name as MaterialName,

      unit,

      @Semantics.amount.currencyCode: 'currency_code'
      unit_price,

      stock_quantity,

      @Consumption.valueHelpDefinition: [{ entity: { name: 'ZI_SO_EXCHANGE_RATE_19', element: 'from_currency' } }]
      currency_code
}
