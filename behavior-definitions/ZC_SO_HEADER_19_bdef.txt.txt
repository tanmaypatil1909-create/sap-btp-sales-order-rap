projection;
strict (2);
use draft;
use side effects;

define behavior for ZC_SO_HEADER_19 alias SalesOrder
{
  use create;
  use update;
  use delete;

  use action ReleaseOrder;
use action CompleteOrder;
use action CancelOrder;

  use association _Items { create; with draft; }
  use association _Customer;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}

define behavior for ZC_SO_ITEM_19 alias SalesOrderItem
{
  use update;
  use delete;

  use association _Header { with draft; }
  use association _Material;
}